# Learning-grade plaintext secret.
# In production, you'd use SealedSecrets, External Secrets Operator, or sops
# instead of committing the raw password to git.
apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: counter-app
type: Opaque
stringData:
  REDIS_PASSWORD: redis-learning-pw-change-me
